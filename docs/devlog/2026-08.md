# 开发日志 2026-08

## 2026-08-03

### Classic→Default 主题迁移 P0–P3 收尾（分支 codex/classic-default-parity，未合 main）

**问题 / 起因**：目标是把 Classic 主题的已实现能力无缝迁移到 Default 主题。本会话以 Codex（审察/验收）+ Pi（实现）协作模式推进，全部改动落在隔离分支 `codex/classic-default-parity`，HEAD 基线 `99bfed82`（v1.0.22），未 push / 未合 main。

**完成批次与提交**（按提交顺序，共 20 commits）：

| Commit | 内容 |
|---|---|
| `34049af9` | chore: vitest 组件测试基建 |
| `a5d334be` | feat: P0 Classic→Default 路由/主题切换兼容 |
| `d9035a4e` | feat: P1-A Docs 全量迁移（14 页 × 7 语言）|
| `5c4b7f06` | fix: 静态资源限流旁路（修 Docs chunk 429→500）|
| `0e331444` | feat: P1-B Default zh-TW i18n |
| `d1cd20cd` | feat: P2-A1 PayPal 完整闭环 |
| `a0a06b9e` | feat: P2-A2 Telegram 前端登录/注册/绑定 |
| `380c0335` | feat: Telegram 未注册自动注册登录 |
| `33f5e399` | fix: 软删后可重注册 + OAuth 新用户默认 Token |
| `12284574` | fix: 默认 Token 名改 `default` |
| `ba0781a8` | feat: P3 Playground 四项（TTS 音色/图片渲染/任务提示/粘贴上传）|
| `b11087b1` | fix: 图片生成不传 size（用上游默认）|
| `1fedccd5` | fix: 粘贴图在聊天气泡显示 |
| `17ebaa2f` | fix: 对话区分 + 图片预览 v4 + 按钮精简 |
| `b5894ea0` | fix: assistant 左 / user 右对齐 |
| `836330a6` | fix: 图片预览弹窗全断点（修 sm:max-w-sm 锁宽）|
| `f7ab282b` | fix: 移除未实现的 Upload file |
| `9ed742a5` | fix: 移除 3D 冗余 URL 输入框 |
| `6f7bec83` | feat: 任务视频页内预览弹窗（对齐 Classic）|
| `3a768214` | fix: 任务日志区分 Image to Video / Image to 3D |

**关键决策**：
- size 策略：playground 无 size UI，永不传 size，用各模型上游默认（避免追上游文档）。
- 对话区分：AI Elements Message 基类对 assistant 用 `flex-row-reverse`，反转行里 `justify-start` 落在视觉右侧；加 `flex-row` 抵消 + DOM 顺序控制。
- 弹窗锁宽：`ui/dialog.tsx` 基类 `sm:max-w-sm` 在 ≥640px 视口胜出；用带 `sm:` 前缀的覆盖类解决。
- 3D 不做 in-browser 预览（model-viewer/three.js 为独立大功能），保留直连下载。

**本地验证**：本地 Docker（`127.0.0.1:3000`）+ cloudflared 隧道实测：Telegram 自动注册/软删重注册、PayPal sandbox 实付入账、图片/视频/3D 生成、任务预览。
- 本地 DNS 被代理拦截返回 `198.18.x.x`，触发视频/3D `/content` 代理的 SSRF 误报；本地设 `fetch_setting.apply_ip_filter_for_domain=false` 解决（仅本地运行时配置，不改代码；生产保持默认）。

**回归**：前端 `npm test` 181/181 全绿；后端 `go build`/`go vet` 通过；`go test ./controller` 仅 1 个既有失败 `TestListModelsTokenLimitIncludesTieredBillingModel`（Redis 空指针，与本批无关，revert 后同样 FAIL）。

**状态**：分支 20 commits，工作区 clean，未 push / 未合 main。下一步：push 分支 + 开 PR，再继续 3D in-browser 预览 / Playground 进阶。
