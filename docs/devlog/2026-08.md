# 开发日志 2026-08

## 2026-08-04

### LongCat 渠道类型 + DB 迁移脚本

**任务**：加回 LongCat 渠道类型，从 100 起编号避免后续升级冲突

**改动**：
- constant/channel.go：ChannelTypeLongcat=100，ChannelTypeDummy 显式改为 101（循环上界覆盖 LongCat），ChannelTypeNames 添加 "LongCat"，ChannelBaseURLs 通过 init() 动态扩展
- common/api_type.go：LongCat 映射到 APITypeOpenAI
- model/migrate_longcat_channel_type.go（新建）：幂等迁移脚本，将 type=58 且 base_url/name 含 longcat 的渠道更新为 type=100，在 AutoMigrate 之前执行
- model/main.go：注册 MigrateLongcatChannelType() 调用

**验证**：
- go build ✅ | go test 32 包 0 失败 ✅
- Docker 重建后迁移自动执行：日志 [MIGRATION] Migrating 1 LongCat channel(s) from type=58 to type=100
- DB 验证：channel id=8 type 58→100 ✅
- 渠道管理页面 LongCat 显示正常 ✅

### rc.23 上游合并（分支 upgrade/rc23）

**任务**：从 v1.0.26（fork 基线 d385d7ab, 2026-03-17）合并上游 QuantumNous/new-api v1.0.0-rc.23（708 commits, 2026-08-01）

**合并策略**：git merge -X theirs — 接受上游全部冲突解决，Vancine 定制后续逐个 reapply

**新增（来自上游 rc.11→rc.23）**：
- 认证系统：JWT access token（15min TTL）+ 服务端 session 表（user_sessions）+ refresh cookie（30天 TTL）+ auth_flows OAuth state 表
- relaykit 模块（132 文件，独立 Go module，协议转换引擎 chat↔responses↔gemini）
- 渠道类型：AdvancedCustom=58, Sub2API=59, NewAPI=60
- GPT-5.6 计费 + 缓存写计费、Claude Opus 4.8、自动组路由、ClickHouse 日志、系统任务执行器 + 实例面板、Zstd 压缩、每渠道 HTTP 传输控制
- 前端：web/default/ → web/ 目录上移、Playground 参数面板、notranslate 浏览器翻译防护、Sankey 流量流图

**删除（废弃）**：Classic 主题（225 文件）、Acquisition 归因系统、响应体捕获中间件、frontend 营销站
**删除（待后续 reapply）**：PayPal 支付、Google OAuth、SiliconFlow 3D 适配器、品牌替换（23 Go 文件）、i18n 懒加载、暗色主题 FOUC 修复、Playground 定制
**修复**：Dockerfile ENTRYPOINT /vancine → /new-api（合并残留，非冲突行从 fork 侧存活）

**验证**：
- go build ./... ✅ 零错误
- go test ./... ✅ 32 包 0 失败
- bun run build ✅ 57294 kB
- Docker 容器 healthy ✅
- 生产 DB 恢复 16MB ✅，AutoMigrate 自动创建 user_sessions/auth_flows 等新表
- API /api/status success=true，system_name=Vancine，theme=default

**已知偏差**：
- ~~LongCat 渠道类型未加回~~ ✅ 已在 Task 2 中恢复（type=100 + 迁移脚本）
- SiliconFlow 3D 被删（指令说保留修复，PiAgent 判定损坏后删除，后续 reapply）
- docker-compose.yml 硬编码 SQL_DSN 密码 123456 覆盖 .env（后续任务修正）

**后续 reapply 任务清单**：
1. ~~LongCat 渠道 type=100 + DB 迁移脚本~~ ✅
2. PayPal 支付（6 文件）
3. Google OAuth 重写（适配 JWT 认证）
4. SiliconFlow 3D 适配器（3 文件）
5. 品牌替换（23 Go 文件）
6. i18n 懒加载（语言码改 zhCN/zhTW）
7. 暗色主题 FOUC 修复
8. 火山方舟 TTS 定制 reapply
9. 文档页迁移（web/default/src/routes/docs/ → web/src/routes/docs/）
10. 跨子域 cookie + 域名迁移 api.vancine.com
11. docker-compose.yml 硬编码密码修正 + 端口改回 3000

### PayPal 支付系统迁移（rc.23 reapply）

**任务**：从 main 分支恢复 PayPal 支付系统，适配 rc.23 新架构

**后端恢复（6 文件）**：
- controller/topup_paypal.go — PayPal 支付控制器（创建订单、webhook、return）
- controller/topup_paypal_validation.go — PayPal 数据验证
- controller/topup_paypal_test.go — PayPal 控制器测试
- model/topup_paypal_settlement.go — PayPal 结算账本（退款/撤销幂等处理，适配 lockForUpdate）
- model/topup_paypal_settlement_test.go — PayPal 结算测试
- setting/payment_paypal.go — PayPal 配置设置

**后端适配**：
- model/topup.go：添加 PaymentId/TransactionId 字段、PaymentMethodPayPal/PaymentProviderPayPal 常量、GetTopUpByTransactionId/FindTopUpByPaymentID/RefundPayPalTopUp/RechargePayPal/CleanExpiredPendingTopUps 函数
- common/constants.go：添加 TopUpStatusRefunded 常量
- router/api-router.go：添加 4 条 PayPal 路由（webhook/return/amount/pay）
- model/main.go：AutoMigrate 添加 PayPalSettlementEvent
- model/option.go：添加 12 个 PayPal 设置 case（初始化 + updateOptionMap switch），修复 DB 设置无法加载到 setting 变量的问题
- controller/topup.go：添加 enable_paypal_topup 到 topup info 响应
- controller/payment_webhook_availability.go：添加 isPayPalTopUpEnabled/isPayPalWebhookConfigured/isPayPalWebhookEnabled 函数

**前端恢复和适配**：
- web/src/features/system-settings/integrations/paypal-settings-section.tsx + test — PayPal 设置组件
- web/src/features/wallet/lib/validate-payment-url.ts — 支付 URL 安全验证
- web/src/features/wallet/constants.ts — PAYPAL 加入 PAYMENT_TYPES 和 PAYMENT_ICON_COLORS
- web/src/features/wallet/types.ts — PayPalPaymentResponse 类型 + paypal_min_topup 字段
- web/src/features/wallet/api.ts — calculatePayPalAmount/requestPayPalPayment API
- web/src/features/wallet/lib/payment.ts — isPayPalPayment/resolvePayPalRedirect + getDefaultPaymentType/getMinTopupAmount 更新
- web/src/features/wallet/lib/ui.tsx — PayPal 图标（SiPaypal）
- web/src/features/wallet/hooks/use-payment.ts — PayPal 支付处理分支
- web/src/features/system-settings/types.ts — PayPal 设置字段
- web/src/features/system-settings/billing/index.tsx + section-registry.tsx — PayPal 默认值
- web/src/features/system-settings/integrations/payment-settings-section.tsx — PayPal 标签页
- web/src/features/wallet/components/recharge-form-card.tsx — hasConfigurableTopup 加入 enable_paypal_topup + PayPal 按钮渲染区块 + “无支付方式”条件更新

**验证**：
- go build ✅ | go test 32 包 0 失败 ✅ | bun run build 57301 kB ✅
- Docker 容器 healthy ✅，PayPalSettlementEvent 表自动创建 ✅
- 钱包页面 PayPal 支付按钮显示 ✅
- 支付路由链路完整：handlePaymentMethodSelect → calculatePayPalAmount → processPayment → requestPayPalPayment → resolvePayPalRedirect ✅

**已知限制**：PayPal 完整支付流程（实际扣款）等所有升级项完成后统一测试

## 2026-08-03

### Classic→Default 主题迁移 P0–P3 收尾（分支 codex/classic-default-parity，未合 main）

**问题 / 起因**：目标是把 Classic 主题的已实现能力无缝迁移到 Default 主题。本会话以 Codex（审察/验收）+ Pi（实现）协作模式推进，全部改动落在隔离分支 `codex/classic-default-parity`，HEAD 基线 `99bfed82`（v1.0.22），未 push / 未合 main。

**完成批次与提交**（按提交顺序，共 20 commits）：

| Commit | 内容 |
|---|---|
| `34049af9` | chore: vitest 组件测试基建 |
| `a5d334be` | feat: P0 Classic→Default 路由/主题切换兼容 |
| `d9035a4e` | feat: P1-A Docs 全量迁移（14 页 × 7 语言）|
| `5c4b7f06` | fix: 静态资源限流旁路（修 Docs chunk 429→500）|
| `0e331444` | feat: P1-B Default zh-TW i18n |
| `d1cd20cd` | feat: P2-A1 PayPal 完整闭环 |
| `a0a06b9e` | feat: P2-A2 Telegram 前端登录/注册/绑定 |
| `380c0335` | feat: Telegram 未注册自动注册登录 |
| `33f5e399` | fix: 软删后可重注册 + OAuth 新用户默认 Token |
| `12284574` | fix: 默认 Token 名改 `default` |
| `ba0781a8` | feat: P3 Playground 四项（TTS 音色/图片渲染/任务提示/粘贴上传）|
| `b11087b1` | fix: 图片生成不传 size（用上游默认）|
| `1fedccd5` | fix: 粘贴图在聊天气泡显示 |
| `17ebaa2f` | fix: 对话区分 + 图片预览 v4 + 按钮精简 |
| `b5894ea0` | fix: assistant 左 / user 右对齐 |
| `836330a6` | fix: 图片预览弹窗全断点（修 sm:max-w-sm 锁宽）|
| `f7ab282b` | fix: 移除未实现的 Upload file |
| `9ed742a5` | fix: 移除 3D 冗余 URL 输入框 |
| `6f7bec83` | feat: 任务视频页内预览弹窗（对齐 Classic）|
| `3a768214` | fix: 任务日志区分 Image to Video / Image to 3D |

**关键决策**：
- size 策略：playground 无 size UI，永不传 size，用各模型上游默认（避免追上游文档）。
- 对话区分：AI Elements Message 基类对 assistant 用 `flex-row-reverse`，反转行里 `justify-start` 落在视觉右侧；加 `flex-row` 抵消 + DOM 顺序控制。
- 弹窗锁宽：`ui/dialog.tsx` 基类 `sm:max-w-sm` 在 ≥640px 视口胜出；用带 `sm:` 前缀的覆盖类解决。
- 3D 不做 in-browser 预览（model-viewer/three.js 为独立大功能），保留直连下载。

**本地验证**：本地 Docker（`127.0.0.1:3000`）+ cloudflared 隧道实测：Telegram 自动注册/软删重注册、PayPal sandbox 实付入账、图片/视频/3D 生成、任务预览。
- 本地 DNS 被代理拦截返回 `198.18.x.x`，触发视频/3D `/content` 代理的 SSRF 误报；本地设 `fetch_setting.apply_ip_filter_for_domain=false` 解决（仅本地运行时配置，不改代码；生产保持默认）。

**回归**：前端 `npm test` 181/181 全绿；后端 `go build`/`go vet` 通过；`go test ./controller` 仅 1 个既有失败 `TestListModelsTokenLimitIncludesTieredBillingModel`（Redis 空指针，与本批无关，revert 后同样 FAIL）。

**状态**：分支 20 commits，工作区 clean，未 push / 未合 main。下一步：push 分支 + 开 PR，再继续 3D in-browser 预览 / Playground 进阶。

## 2026-08-05

### SiliconFlow 3D 适配器恢复

**任务**：从 main 分支恢复 SiliconFlow 3D 任务适配器（上游无此功能）

**改动**：
- 恢复 relay/channel/task/siliconflow/ 3 文件（adaptor.go, constants.go, models.go）
- relay/relay_adaptor.go 注册 taskSiliconFlow.TaskAdaptor（ChannelTypeSiliconFlow=40）
- Import 适配 relaykit：双 dto 包模式（taskdto + relaykit/dto），与 kling adaptor 一致

**验证**：go build ✅ | go test 0 失败 ✅ | Docker healthy ✅
