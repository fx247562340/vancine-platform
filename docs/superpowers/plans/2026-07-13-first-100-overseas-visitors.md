# First 100 Overseas Visitors Campaign Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish four transparent, tracked acquisition messages that bring qualified overseas developers to the AI Media API landing page.

**Architecture:** Use existing authenticated browser sessions for X and Reddit. Each publication has unique `utm_content` while preserving the approved source, medium, and campaign values. Verify every public result immediately and capture its URL for monitoring.

**Tech Stack:** Agent Reach read-only research, Chrome/CDP browser interaction for publishing, Umami for measurement.

## Global Constraints

- Use only copy approved in `docs/superpowers/specs/2026-07-13-first-100-overseas-visitors-design.md`.
- Disclose founder affiliation in every Reddit publication.
- Do not claim cheapest pricing, official partnership, unrestricted models, safety bypass, or guaranteed availability.
- Do not send DMs, mass reply, upvote, or manipulate engagement.
- Stop if the active account identity is unclear, a platform presents a verification challenge, or a post is removed.

---

### Task 1: Preflight

- [ ] Confirm `https://vancine.com/ai-media-api` and all four UTM links return HTTP 200.
- [ ] Confirm the current X and Reddit account identities before publishing.
- [ ] Confirm the two target Reddit posts still exist and accept comments.

### Task 2: Publish X launch

- [ ] Create one X post using the exact approved X copy.
- [ ] Verify the public post URL, text, and UTM link.
- [ ] Do not create a thread unless the single post exceeds the platform limit.

### Task 3: Publish two high-intent Reddit replies

- [ ] Reply to Reddit post `1sm2bfo` with the approved reliable-Seedance copy.
- [ ] Reply to Reddit post `1sginug` with the approved individual-options copy.
- [ ] Verify both replies are visible while logged out or through a public read.

### Task 4: Publish r/SideProject launch

- [ ] Create one text post in `r/SideProject` using the approved title and body.
- [ ] Verify the post is public and record its URL.

### Task 5: Record and monitor

- [ ] Report account identities, publication URLs, timestamps, and any platform warnings.
- [ ] Confirm no unapproved engagement action was taken.
- [ ] Review Umami traffic at 24 hours and 72 hours using the UTM values.
